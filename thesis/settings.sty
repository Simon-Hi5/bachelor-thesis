% **************************************************
% Packages
% **************************************************

% Use UTF-8 encoding
\usepackage[utf8]{inputenc}

% Use german language package
\usepackage[ngerman]{babel}

% Reference the number of pages
\usepackage{lastpage}

% Style header and footer
\usepackage{fancyhdr}

% Embed pictures
\usepackage{graphicx}

% Embed tables
\usepackage{tabularx}

% Embed source code
\usepackage{listings}

% Use different colors
\usepackage[usenames,dvipsnames,table]{xcolor}

% Embed urls
\usepackage[urlcolor=blue, pdfborder={0 0 0}]{hyperref}

% Use multiple columns
\usepackage{multicol}

% Add list of abbreviations
\usepackage{acronym}

% Improve figure positioning
\usepackage{float}

% Use Helvetica font
\usepackage[scaled]{helvet}

% Use Times New Roman font
\usepackage{mathptmx}

% Add figure captions
\usepackage[font=small]{caption}


% **************************************************
% Bibliography
% **************************************************

% Use biblatex with biber for bibliography
\usepackage[backend=biber, bibstyle=authoryear, citestyle=authoryearbrackets, maxcitenames=2, sorting=none]{biblatex}

% Add source for bibliographic database
\addbibresource{bibliography.bib}

% Provide advanced quotations
\usepackage{csquotes}

% Spacing between entries
\setlength\bibitemsep{1.5\itemsep}


% **************************************************
% Source code formatting
% **************************************************

\renewcommand\lstlistingname{Quelltext}

\renewcommand\lstlistlistingname{Quelltextverzeichnis}

\definecolor{backcolour}{HTML}{F2F2F2}
\lstset{
    backgroundcolor=\color{white},
    basicstyle=\footnotesize\ttfamily,
    breaklines=true,
    captionpos=b,
    commentstyle=\color{Gray},
    escapeinside={\%*}{*)},
    keywordstyle=\color{MidnightBlue},
    stringstyle=\color{OliveGreen},
    showstringspaces=false,
    showspaces=false,
    numbers=left,
    numberstyle=\tiny,
    aboveskip=20pt,
    belowskip=20pt,
    showtabs=false,   
    breaklines=true         
}

\lstdefinelanguage{Dockerfile}
{
  morekeywords={FROM, RUN, CMD, LABEL, MAINTAINER, EXPOSE, ENV, ADD, COPY, ENTRYPOINT, VOLUME, USER, WORKDIR, ARG, ONBUILD, STOPSIGNAL, HEALTHCHECK, SHELL},
  morecomment=[l]{\#},
  morestring=[b]"
}

\lstdefinelanguage{YAML}
{
  morekeywords={apiVersion, kind, metadata, name, data, app, spec, selector, replicas, template, containers, image, imagePullPolicy, containerPort, env, valueFrom, configMapKeyRef, key, resources, limits, memory, cpu, requests, type, nodePort, type, protocol, ports, port, matchLabels, labels, value, scaleTargetRef, minReplicas, maxReplicas, targetCPUUtilizationPercentage},
  morecomment=[l]{},
}

\lstdefinelanguage{JavaScript}{
  keywords={break, case, catch, continue, debugger, default, delete, do, else, false, finally, for, function, if, in, instanceof, new, null, return, switch, this, throw, true, try, typeof, var, void, while, with},
  morecomment=[l]{//},
  morecomment=[s]{/*}{*/},
  morestring=[b]',
  morestring=[b]",
  ndkeywords={class, export, boolean, throw, implements, import, this},
  sensitive=true
}

% **************************************************
% Header and footer
% **************************************************

\pagestyle{fancy}
\fancyhf{}
\lhead{\nouppercase{\leftmark}}
\chead{}
\rhead{}
\lfoot{}
\cfoot{\thepage}
\rfoot{}

% **************************************************
% Miscellaneous
% **************************************************

% Reduce hyphenation
\hyphenpenalty=9000
\tolerance=3000

% Avoid widows and orphans
\widowpenalty=9999
\clubpenalty=9999
\displaywidowpenalty=9999

% Styling of quote at start of chapter
\setkomafont{dictumtext}{\itshape}
\setkomafont{dictumauthor}{\normalfont}
\renewcommand*\dictumwidth{\linewidth}
\renewcommand*\dictumauthorformat[1]{-- #1}
\renewcommand*\dictumrule{}